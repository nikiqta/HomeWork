{{> header}}
<div class="container body-content">
    <div class="row">
        <div class="col-md-6">
            {{#each articles}}
                {{> article}}
            {{else}}
                <div>
                    <h3>
                        There are currently no articles yet...!
                    </h3>
                </div>
            {{/each}}
        </div>
    </div>
</div>
{{> footer}}